@using CSharp___WebBlog.Models
@using CSharp___WebBlog.Services.Iterfaces
@model IEnumerable<Blog>
@inject IImageService ImageService

@{
    ViewData["Title"] = "Index";
}

<h1>Blogs Index</h1>

<p>
    <a asp-action="Create">Create New Blogs</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Image
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.Description)*@
                Description
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Created)
            </th>
            <th>
                @*@Html.DisplayNameFor(model => model.Updated)*@
                Last Updated
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BlogUser)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var blog in Model)
        {
            <tr>
                <td>
                    <img src="@ImageService.DecodeImage(blog.ImageData, blog.ImageType)">
                </td>
                <td>
                    @Html.DisplayFor(modelItem => blog.Name)
                </td>
                <td>
                    @*@Html.DisplayFor(modelItem => blog.Description)*@
                    Description
                </td>
                <td>
                    @Html.DisplayFor(modelItem => blog.Created)
                </td>
                <th>
                    @Html.DisplayNameFor(model => model.Updated)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.BlogUser.Id)
                </th>
                <td>
                    <a asp-action="Edit" asp-route-id="@blog.Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@blog.Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@blog.Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>